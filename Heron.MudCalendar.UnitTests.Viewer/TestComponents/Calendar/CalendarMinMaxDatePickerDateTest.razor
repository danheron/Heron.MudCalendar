<MudPopoverProvider />

<MudCalendar T="CalendarItem" ShowDatePicker="true" ShowToolbar="true" @bind-CurrentDay="@_currentDay" DatePickerMinDate="@_minDate" DatePickerMaxDate="@_maxDate" />

@code {

    public static string __description__ = "MinMax Date Picker Date Tests";

    private DateTime _currentDay = new DateTime(2025, 2, 12);
    private DateTime? _minDate = new DateTime(2024, 1, 1);
    private DateTime? _maxDate = new DateTime(2026, 12, 31);
}
