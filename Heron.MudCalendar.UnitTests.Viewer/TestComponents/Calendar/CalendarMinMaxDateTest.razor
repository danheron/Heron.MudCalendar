<MudPopoverProvider />

<MudCalendar T="CalendarItem" ShowDatePicker="true" @bind-CurrentDay="@_currentDay" MinDate="@_minDate" MaxDate="@_maxDate" />

@code {

    public static string __description__ = "MinMax Date Test";

    private DateTime _currentDay = new DateTime(2025, 1, 1);
    private DateTime? _minDate = new DateTime(2024, 1, 1);
    private DateTime? _maxDate = new DateTime(2026, 12, 31);
}
